# Opt-in accessibility audit profile: `just axe` (or QUARTO_PROFILE=a11y quarto render).
# Kept out of index.qmd so the deployed deck never ships the axe-core payload.
format:
  revealjs:
    axe:
      output: document
