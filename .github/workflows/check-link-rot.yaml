name: Check Link Rot

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:

jobs:
  check-link-rot:
    uses: IndrajeetPatil/workflows/.github/workflows/check-link-rot.yaml@main
    permissions:
      contents: read
